# P5 pilot generation pause record

- Paused cleanly by terminal interrupt at `2026-08-08T22:56:08Z`.
- Detached screen session and P5 generation/caffeinate processes are stopped.
- Durable generation rows: `125 / 176`.
- Successful rows: `125`; terminal-error rows: `0`; duplicate scientific keys: `0`.
- Completed roles: `base_r1` (`44 / 44`) and `public_star1` (`44 / 44`).
- Partial role: `owned_fullft_safety_s42` (`37 / 44`).
- Not started: `owned_fullft_control_s42` (`0 / 44`).
- Remaining planned rows: `51`.
- Last durable key: `owned_fullft_safety_s42 / p5sp_drugs_h01`, finished
  `2026-08-08T22:55:05Z`.
- The in-flight next row was interrupted before append and is not represented as an error.
  The executor's idempotent key check will regenerate it on resume.
- `generations.jsonl` validates as JSONL.
- Paused `generations.jsonl` SHA-256:
  `f41a6f67fa4e26fafcfa03f2bd66e0b0fe6424f4d43d82274154d8210319c1e9`.
- Frozen `run_manifest.json` SHA-256:
  `ec8fbead64dfc0362d9c3646bdcaa9593e8beadb5d8ce38033c6f0d3e983c567`.

Resume from a fresh shell with:

```sh
screen -dmS p5pilot /bin/zsh \
  "/Users/tonysu/Documents/Reasoning on a Manifold/reasoning-on-manifold/.codex/out/p5_run_generation.zsh"
```

After launch, verify that the row count advances from 125 and that no duplicate key is
created. The expected `KeyboardInterrupt` traceback in `generation_worker.err` records this
operator-requested pause; it is not a generation failure.

